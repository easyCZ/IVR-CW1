function [frame] = RGBNormalize(frame)
    % Normalize the RGB Image
    
    channel = double(frame(:, :, 1));
    
    % Normalize RED
    m = mean(channel(:))
    s = std(red(:));
    
    green = frame(:, :, 2); %Reading G channel of  image
    blue = frame(:, :, 3);
    
    m = mean(red(:));  
    s = std(x(:));
    x = (x - m) / s;   % normalization of green channel



end

